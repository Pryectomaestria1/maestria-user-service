generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model UserProfile {
  userId    String @id
  name      String
  avatarUrl String @default("")
}

model UserRole {
  userId String @id
  role   String @default("Student")
}
